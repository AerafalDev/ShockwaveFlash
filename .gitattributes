# Check out every text file with CRLF on all platforms (Windows default);
# the repository normalizes to LF on commit.
* text=auto eol=crlf

*.cs       diff=csharp
*.cshtml   diff=html
*.csx      diff=csharp
